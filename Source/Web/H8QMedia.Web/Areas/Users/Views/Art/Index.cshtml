@model H8QMedia.Web.ViewModels.Art.ArtIndexViewModel
@{
    ViewBag.Title = "Art";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2 class="text-center">My Arts</h2>

<div class="container">
    <div calss="row text-center">
        <div class="col-md-12">
            <a href="/Users/Art/Create" class="btn btn-md btn-primary full-width">Create new Art</a>
        </div>
    </div>
    <hr/>

    @Html.Partial("_ArtPaginationPartial", Model)

    <div class="row">
        @foreach (var art in Model.ArtArticles)
        {
            <div class="col-md-6">
                @Html.DisplayFor(model => art, DisplayTemplatesPaths.ArticleViewModel)
            </div>
        }
    </div>

    @Html.Partial("_ArtPaginationPartial", Model)

    @*<div class="row text-center">
        <div class="col-md-12">
            <ul class="pagination">

                @if (Model.CurrentPage > 1)
                {
                    <li><a href="/Users/Art/Index/@(Model.CurrentPage - 1)">&laquo;</a></li>
                }

                @for (int i = 1; i <= Model.TotalPages; i++)
                {
                    var className = Model.CurrentPage == i ? "active" : string.Empty;

                    <li class="@className"><a href="/Users/Art/Index/@i">@i</a></li>
                }

                @if (Model.CurrentPage < Model.TotalPages)
                {
                    <li><a href="/Users/Art/Index/@(Model.CurrentPage + 1)">&raquo;</a></li>
                }
            </ul>
        </div>
    </div>*@
</div>
